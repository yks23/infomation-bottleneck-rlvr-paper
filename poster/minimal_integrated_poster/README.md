# Minimal Integrated Poster

Primary files:

- `minimal_information_budget_poster.png`
- `minimal_information_budget_poster.pdf`
- `build_minimal_poster.py`

Regenerate:

```bash
python3 poster/minimal_integrated_poster/build_minimal_poster.py
```

Story:

```text
AlphaZero renews a 50/50 frontier.
Fixed LLM math RLVR spends a finite useful verifier source.
RL sharpens broad rollout distributions into problem seeds.
Transfer geometry measures how those seeds interact.
Diffusion RL is a supporting contrast for richer but alignment-sensitive reward channels.
```
